

function showAge (name, age){
    return `${name} is ${age} years old.`;
}

function showTown (name, town){
    return `${name} lives in ${town}.`;
}

function showHobby (name,hobby){
    return `${name} likes ${hobby} as a hobby.`;

}

export {showAge, showTown, showHobby};